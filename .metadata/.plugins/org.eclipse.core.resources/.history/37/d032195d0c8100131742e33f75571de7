package com.jonathan.survivor.entity;

import com.jonathan.components.Position;


public class EntityManagerTest 
{
	/*public static void main(String[] args)
	{
		EntityManager manager = new EntityManager();
		
		int object = manager.createEntity();
		manager.addComponent(object, new Position());
		Position objectPosition = manager.getComponent(object, Position.class);
		objectPosition.x = 50;
		
		int object2 = manager.createEntity();
		manager.addComponent(object, new Position());
		Position object2Position = manager.getComponent(object, Position.class);
		object2Position.x = 50;
		
		manager.destroyEntity(object);
		System.out.println(manager.getAllComponents(Position.class));
	}*/
}
