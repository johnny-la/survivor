package com.jonathan.survivor;

public class MeleeWeapon extends Weapon
{
	/** Stores the range in world units of the melee weapon. */
	private float range;
	
	/** Accepts the damage, range, name and the description of the melee weapon. */
	public MeleeWeapon(float damage, float range, String name, String description)
	{
		super(damage, name, description);
		
		this.range = range;
	}

	/** Gets the range in world units of the melee weapon. */
	public float getRange() {
		return range;
	}

	/** Sets the range in world units of the melee weapon. */
	public void setRange(float range) {
		this.range = range;
	}
	
	
}
